import ollama
import json
from typing import List, Dict
from pathlib import Path


def generate_population_sample(n: int) -> List[List[str]]:
    """
    Returns a list of string attributes for `n` many agents. Uses iterative proportional fitting.
    """
    pass


def prepare_survey_questions(population_sample):
    pass


def query_sample(population_sample: List[List[str]], user_messages: List[Dict[str]]):
    pass